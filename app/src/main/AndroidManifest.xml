<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="org.probuilder.pgquizz">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/cn"
        android:label="@string/app_name"
        android:roundIcon="@drawable/cn"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".BookPdf"></activity>
        <activity android:name=".CounselingActivity" />
        <activity android:name=".StudentInfo" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".StudentRegisterActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".FavoriteQuestionActivity" />
        <activity android:name=".ScoreActivity" />
        <activity android:name=".QuestionsActivity" />
        <activity android:name=".SetsActivity" />
        <activity android:name=".CategoriesActivity" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>